# 開発メモ

pg_quiz の保守・問題追加のための覚書です。利用者向けの説明は [README.md](README.md) を参照してください。

## 構成

アプリはビルド不要の素の HTML / CSS / JavaScript です。依存パッケージはありません。

```
pg_quiz/
├── index.html                … 読み込むスクリプトの一覧（questions は format.js が自動更新）
├── manifest.webmanifest      … ホーム画面に追加したときの名前・アイコン
├── sw.js                     … Service Worker（オフライン用のキャッシュ）
├── icons/                    … アイコン（tools/make-icons.js で生成）
├── css/style.css
├── js/
│   ├── config.js             … アプリ設定（version、repoUrl）
│   ├── app.js                … 画面・ロジック
│   ├── store.js              … localStorage 保存
│   └── data/
│       ├── categories.js     … 出題範囲（カテゴリ・重要度）、Ver差分カテゴリ
│       ├── references.js     … 参考ページ
│       └── questions/        … 問題データ
│           ├── S1.js S2.js S3.js           … Silver
│           ├── G1.js G2.js G3.js G4.js     … Gold
│           └── V1.js                       … バージョン差分（試験範囲外）
├── tools/                    … 開発用（Node.js）。アプリの動作には使わない
│   ├── lib.js                … 共通（データファイルの読み込み）
│   ├── plan.js               … 収録計画（トピックごとの目標問題数の概算）
│   ├── serve.js              … ローカル確認用サーバ
│   ├── validate.js           … 問題データの検証・リンク確認・収録状況の表示
│   ├── format.js             … 問題データの整形・ファイル分割
│   ├── terms.js              … 公式の出題範囲の用語が、そのレベルの問題に登場するかの確認
│   └── make-icons.js         … icons/*.png の生成
└── package.json
```

ブラウザが読み込むのは `index.html` / `manifest.webmanifest` / `sw.js` / `css/` / `js/` / `icons/` だけです（約 980KB、うち問題データが約 860KB）。
`tools/` と `README.md`、`DEVELOPMENT.md`、`package.json` は配信されても使われません。

## コマンド（Node.js 18 以降）

```bash
npm run serve       # http://localhost:8765/ で確認
npm run validate    # 問題データの形式チェックとカテゴリ別の収録状況
npm run check       # 上記 + 解説リンク（ページとアンカー）の存在確認
npm run format      # 問題データをカテゴリ順に整形して questions/*.js に書き出す
node tools/terms.js       # 公式の出題範囲の用語ごとの登場数（--all で全件）
node tools/make-icons.js  # アイコンを作り直す
```

`validate` が確認する内容:

- id の重複、カテゴリとレベル・id 接頭辞の整合、選択肢が5つで重複がないこと、answer の範囲、解説と refs の有無
- 問題文の「Nつ選びなさい」と answer の個数が一致していること
- 問題文が「前問」など他の問題を前提にしていないこと（出題順はランダムのため、問題は単独で成立させる）
- index.html に読み込まれていない問題ファイル
- 警告: 正解の選択肢だけが極端に長い（推測されやすい）、カテゴリの収録数が重要度に足りない、収録計画がないカテゴリ

リンク確認の結果は OS の一時ディレクトリに7日間キャッシュされます。

## 学習データの保存形式

学習データは localStorage のキー `pg_quiz` に保存します。形式は `js/store.js` の `SCHEMA` で管理します。

- 解答は `log`（`[時刻ms, 問題ID, 1|0]` の配列）にすべて記録し、問題ごとの成績 `qstats` と日別の解答数 `daily` は `log` から集計します。エクスポートには集計値を含めません。
- 統合インポートは、`log` を「時刻と問題ID」、`history` を `id` で重複排除して和集合をとり、集計し直します。何度統合しても、どちらの向きに統合しても結果は同じです。
- インポートとリセットの直前のデータを `pg_quiz_backup` に1世代だけ保存します。
- インポートするファイルは外部からの入力として扱い、`store.js` の `sanitize()` で全項目の型と許容値（モード・レベル・出題タイプ・問題IDの書式・選択肢番号など）を確認します。想定外の値が1つでもあれば、ファイル全体を読み込みません。中断中のクイズは書き出さず、読み込みもしません。
  **モードやレベル、設定項目を追加したときは、`store.js` の許容値の一覧も更新してください**（更新しないと、新しい値を含むファイルをインポートできません）。
- 画面に出す文字列は、保存データ由来のものも `esc()` を通します。

形式を変えるときは、利用者の学習データを失わないように次のようにします。

1. `SCHEMA` を1つ上げる
2. `MIGRATIONS` に「1つ前の形式 → 新しい形式」の変換を追加する（保存データ・エクスポートファイル・直前の状態のいずれにも適用されます）
3. 旧形式のエクスポートファイルを用意し、「統合」「置き換え」と再読み込みで変換されることを確認する

変換できない形式のデータは、読み込み時に初期状態として扱います。
逆に、キャッシュされた古いアプリが新しい形式のデータを読んだ場合は、上書きしないように保存・インポート・リセットを止め、再読み込みを促します。

## オフライン対応

`sw.js` はインストール時に `index.html` を取得し、その中の `src` / `href` が指す同一サイト内のファイルをキャッシュします。index.html に読み込みを追加すれば、キャッシュ対象にも自動で加わります。
取得はネットワーク優先（4秒で打ち切り）で、成功したらキャッシュを更新し、失敗したときだけキャッシュを返します。そのため問題データを更新しても、利用者側で特別な操作は要りません。
キャッシュ名は `pg_quiz-v1` です。GitHub Pages では同じユーザーの別リポジトリのサイトとオリジン（Cache Storage）を共有するため、古いキャッシュを消すときは `pg_quiz-` で始まる名前だけを対象にしています。
Service Worker は http / https で開いたときだけ登録されます（`index.html` を直接開いた場合は登録されません）。

## 問題の追加

1. `js/data/questions/` の該当ファイル（例: G1.2 なら `G1.js`）の末尾の `);` の前に追加します。
   別ファイルにまとめて書いた場合は `node tools/format.js 追加ファイル.js` で取り込めます。
2. `npm run format` で並べ替え、`npm run check` でエラーがないことを確認します。

```js
{
  id: 'G1.2-012',            // 一意なID（カテゴリ-連番）
  level: 'gold',             // 'silver' | 'gold' | 'ver'
  cat: 'G1.2',               // categories.js のカテゴリID
  type: 'scenario',          // 任意: 状況判断の問題（知識確認なら省略）
  q: '問題文。`code` はインラインコード表示、**強調** は赤太字',
  code: 'SELECT 1;',         // 任意: コードブロック
  choices: ['A', 'B', 'C', 'D', 'E'],  // 5つ
  answer: 2,                 // 正解のインデックス（0始まり）
                             // 複数選択は配列で [0, 3] のように書く（2〜3個）
  shuffle: false,            // 任意: 選択肢をシャッフルしない（数値の昇順など）
  exp: '解説文（\\n で改行）',
  refs: [
    ['VACUUM', 'sql-vacuum.html'],             // PG14文書はファイル名#アンカー
    ['外部サイト', 'https://example.com/']      // http から始まるURLはそのまま
  ]
},
```

出題タイプの基準:

- **状況判断（`type: 'scenario'`）**: 設定・パラメータ値、コマンドやビューの出力、ログ、発生している症状、テーブルの中身とそれに対する一連の SQL を示し、結果・原因・対処を判断させる問題
- **知識確認（省略）**: 機能・仕様・既定値・構文、式や関数単体の戻り値を問う問題
- 状況判断の問題に載せる出力やログは、PostgreSQL 14 の実機で採取したものを使います（採取環境は後述）。

作成時の方針:

- 文書のリンクは `https://www.postgresql.jp/document/14/html/` を基準に解決されます。
- アンカーは PG14 日本語文書の ID（例: `runtime-config-wal.html#GUC-WAL-LEVEL`、大文字）を指定します。日本語文書に存在しないアンカーは `npm run check` が検出します。
- バージョン差分の問題は、各バージョンのリリースノートなどを完全な URL で参照します（PG14 の文書には 14.x のリリースノートしか含まれていません）。
- 解説は選択肢ごとに「なぜ正しい／誤りか」が分かるように書きます。
- 正解の選択肢だけが長いと推測できてしまうため、選択肢の長さは揃えます。
- `shuffle: false` の問題（数値や年の昇順など）は、正解位置が特定の記号に偏らないようにします。
- 問題を削除・ID変更すると、その問題の成績は表示されなくなります（中断中セッションからは自動で除外されます）。

## 公式の出題範囲の用語

`tools/terms.js` に、OSS-DB 公式サイトの出題範囲（Silver / Gold）に「重要な用語、コマンド、パラメータなど」として挙がっている用語と、「主要な知識範囲」のうち用語で判定できるものを転記しています。
用語ごとに、同じレベルの問題（問題文・選択肢・コード・解説）に登場する数を数え、0 のものを「未出題」として表示します。問題を削除・変更したときや、公式の出題範囲が改訂されたときに確認してください。

## 収録計画

`tools/plan.js` に、PostgreSQL 14 文書と出題範囲の主要トピックごとの目標問題数（概算）を置いています。
`npm run validate` の出力で、カテゴリごとの収録数と目安を確認できます。
トピックの判定は問題文・選択肢のキーワードによる簡易なもので、1問が複数のトピックに数えられることがあります。

アプリからは読み込まないため、公開時の配信量には影響しません。

## 公開（GitHub Pages）

1. GitHub で新しいリポジトリ（例: `pg_quiz`）を作成する
2. `js/config.js` の `repoUrl` にリポジトリの URL を設定する（「このアプリについて」に Issues へのリンクが表示されます）
3. この `pg_quiz` フォルダ（git リポジトリ、ブランチ `main`）をリポジトリのルートとして push する
   ```bash
   git remote add origin https://github.com/<ユーザー名>/pg_quiz.git
   git push -u origin main
   ```
4. リポジトリの **Settings → Pages** で、Source を `Deploy from a branch`、Branch を `main` / `/ (root)` にして保存する
5. 数分後に `https://<ユーザー名>.github.io/pg_quiz/` で利用できる

ローカルでは `index.html` をブラウザで直接開いても動作します。

`tools/` をリポジトリに含めたくない場合は、`.gitignore` に `tools/` と `DEVELOPMENT.md` を追加してください（その場合、問題の検証・整形はローカルのファイルでのみ行うことになります）。

## 状況判断の問題に使う出力の採取

状況判断の問題に載せている PostgreSQL の出力・ログ・エラーメッセージは、PostgreSQL 14 の実機で採取しています。採取環境はこのリポジトリの外（`../pg_quiz_lab`）にあり、公開対象ではありません。

- Vagrant + VirtualBox の AlmaLinux 9 に、PGDG リポジトリの PostgreSQL 14 を導入
- 1台の VM にプライマリ（5432）、ストリーミングレプリケーションのスタンバイ（5433）、論理レプリケーションのサブスクライバ（5434）、PITR 用（5435）などのクラスタを作成
- `scripts/*.sh` を VM に送って実行し、結果を `out_*.txt` に保存（`run.ps1 <スクリプト名>`）
- `lc_messages = 'C'` にして、サーバのメッセージは英語で採取
- 横に長い表は `narrow.js` で必要な列だけに絞る（値は変えず、psql と同じ書式で組み直す）

問題文では、ログの先頭（時刻・PID など）や一部の列・行を省略することがあります。省略した場合は問題文にそう書きます。

## 文書のカバー状況の調べ方

README に載せているカバー状況は、次の手順で概算したものです。

1. PG14 日本語文書の索引（`bookindex.html`）と目次から、リンクされている `.html` ページを一意に数える
2. 全問題の `refs` と `references.js` から、参照している PG14 文書のページを一意に数える
3. 両者を突き合わせ、参照していないページを接頭辞（`sql-`、`catalog-` など）で分類する
